---
sidebar_position: 6
---

# PHP 垃圾回收

PHP 管理内存的方式是引用计数和写时复制。

- 引用计数：用一个符号表记录和保证变量不再使用后，内存释放给操作系统。
- 写时复制：即在改变对象的属性或数组的成员时，对象或数组才被复制一份占用更多内存。

```php showLineNumbers
if (!isset($v1)) {
    $v1 = "abc";
}

unset($v1);
```
